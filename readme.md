# После клонирования ввести в терминал

``` bash
npm i
```

Запустить проект через сценарии npm или ввести в терминал

``` bash
npm run dev
```

Создать пустой проект

``` bash
npm create vite@latest
```

Отвечаем "y"
Вводим имя проекта
Выбираем vanilla
Выбираем typescript
